version: "3.9"

services:
  csv-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "13000:3000"
    environment:
      PORT: 3000
      HOST: "0.0.0.0"
    volumes:
      - ./public/server-data:/app/public/server-data
    restart: unless-stopped
